# Changelog

## 0.1.0 - Unreleased

- Add validated SQLite composition queries, imports and binary ranking benchmarks.
- Add reproducible Materials Project snapshot audits and positive-unlabelled
  recovery workflows for composition and chemical-system holdouts.
- Add frozen label-sensitivity analyses and a fixed-compute element-pair
  comparator with independent output validation.
- Add MIT licensing, scoped third-party notices, citation metadata and a
  source-release content audit.
- Reconcile preserved MP JSONL records with normalized TSV inputs during audit,
  enforce the exporter metadata contract, and reject special release members.
- Add Julia-native synthetic snapshot generation and external-score coverage
  inspection while retaining Python for the optional MP API adapter and
  independent saved-output validation.
